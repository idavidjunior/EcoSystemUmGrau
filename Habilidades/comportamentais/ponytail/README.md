# Ponytail (habilidade comportamental)

> Estado: **pendente de conteúdo real** — decisão `2026-07-31-habilidades-catalogo-unico-jarvis`.

Local onde o plugin de personalidade "lazy senior dev" deve ser organizado ao ser localizado/implementado:

- `comandos/` — `/preguiça`, `/review`, `/sarcasmo`, `/clean-code`, etc.
- `manifesto.json` — hooks e gatilhos comportamentais (entrypoint do `manifesto_geral.json`).

Referência antiga (não existe mais): `plugins/ponytail/` (vazio) e `config/opencode.jsonc` apontava para `plugins/ponytail/.opencode/plugins/ponytail.mjs`.
