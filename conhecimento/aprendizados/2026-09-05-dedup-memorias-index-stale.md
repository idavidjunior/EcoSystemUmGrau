---
tipo: padrao
tags: [memoria, dedup, index-stale, memory-semantic, tfidf, consolidacao, calibracao]
data: 2026-09-05
contexto: memories.json acumulava duplicatas de memórias (704 entradas, muitas com o mesmo título/summary). Além disso, o índice TF-IDF era reconstruído a cada checagem porque index_stale() retornava sempre True, e o rebuild denso ficava desatualizado.
decisao: Três linhas de ataque. 1) Dedup determinístico: DEDUP_MIN_SCORE foi recalibrado de 0.80 para 0.50 (duplicatas de título idêntico mediam TF-IDF 0.40-0.54; não-duplicatas acima de 0.60 eram 0 de 99 pares amostrados). Camada 1: _normalizar_titulo (título normalizado idêntico -> merge imediato). Camada 2: _normalizar_titulo + busca TF-IDF com threshold 0.50. 2) Consolidação retroativa 704->637 (29 grupos, 67 duplicatas), canônica = mais recente por created_at (lista[-1] do grupo), ids antigos removidos, source_refs desativados via monkeypatch me._enrich_source_refs=lambda task,summary:[] porque SourceRegistry estourava 120s. 3) Bug index_stale: o fingerprint gravado em tfidf_meta.json (JSON) vira lista [count, notas_fp], mas _index_fingerprint() montava TUPLA igual [count, tuple(notas_fp)]; comparar JSON(lista) != python(tupla) é sempre True, então index_stale() sempre sinalizava stale e forçava rebuild TF-IDF a cada chamada. Correção: _index_fingerprint() agora retorna [count, notas_fp.list()] (lista pura). Fix validado: index_stale False, 1660 docs (637 mem + 1023 notas), 0 ausentes, 0 órfãos.
impacto: 637 memórias únicas (erro 137, decisao 180, padrao 236, episodio 58, preferencia 17, experiencia 3, melhoria 1, conteudo 2, 'tipo=erro' 2, None 1). Teste adversarial: 36 memórias multi-kind, 0 pares distintos com score >= 0.50 (sem falsos positivos). Canônica #98291 (Preflight Etico APROVADO, 21 merges) buscada por si mesma: base=0.468 < 0.50 -> a camada semântica não mesclaria nem re-add idêntico; essa proteção vem da camada 1 determinística. Grupo 236/250 mantido excluído (conteúdos distintos). IMPORTANTE: memory_semantic.py NÃO tem subcomando build-index (CLI usa build-dense); python -c com aspas/f-strings quebra no PowerShell (usar .py em Temp). Preflight: 6 erros são baseline pré-existente (node fora do PATH do sandbox para filesystem/search/terminal/github + 3 hardcoded paths históricos em memories.json IDs 98274/98289) -- confirmado por log de execuções anteriores, não é regressão desta tarefa. tfidf_meta.json tem 122 BOMs embutidos (WARN, conteúdo do corpus, não gerado pelo build_index que usa json.dump limpo).
